import XCTest
@testable import NewsApiNetworking

final class NewsApiNetworkingTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(NewsApiNetworking().text, "Hello, World!")
    }
}
